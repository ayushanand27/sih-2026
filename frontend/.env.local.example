# For local development only — copy to .env.local and point at your locally
# running backend. Never read in production: the deployed app gets this from
# the hosting platform's environment variables instead (see frontend/README.md).
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
